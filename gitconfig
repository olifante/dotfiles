[color]
    status = auto
    branch = auto
    diff = auto
    interactive = auto
    # ui = true
[core]
    editor = vim
[user]
    name = Tiago Henriques
    email = tiago-g-henriques@telecom.pt
[github]
    user = olifante
[alias]
    st = status
    ci = commit
    #br = branch
    co = checkout
    df = diff
    dc = diff --cached
    lg = log -p
    lol = log --graph --decorate --pretty=oneline --abbrev-commit
    lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
    ls = ls-files

    # Show files ignored by git:
    ign = ls-files -o -i --exclude-standard

    # Tiago
    b = branch
    ba = branch -a
    br = branch -r
    s = status -sb
    l = log --pretty=oneline --abbrev-commit -10
    last = cat-file commit HEAD
    t = tag
    tl = tag -l
[push]
	default = tracking
